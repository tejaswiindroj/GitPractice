java
tomcat
copy from internet to tomcat webapps folder

java==> Redhat

openjdk8 (java-1.8.0-openjdk.x86_64)

tomcat7 (yum install tomcat)

http://ftp-nyc.osuosl.org/pub/jenkins/war-stable/2.190.3/jenkins.war

/usr/local/Cellar/tomcat/9.0.29/libexec/webapps/   (Tomcat destination in local mac)

ansible -b localhost -m yum -a "name=git"

git checkout "branchname"    -- For chenaging the branch
git merge "branch which you want to get files from to another branch"
example :
   git checkout master
   git merge sprint1
   git push 


   git fetch
git reset --hard origin/master

ansible -b localhost -m yum -a "name=git"    --- installing git in ansible